[pytest]
testpaths = tests
markers =
    unit: no live services required; runs on macOS
    integration: requires orbison box (OPNSENSE_HOST env var)
    packaging: requires OPNsense build tools on orbison
    slow: takes more than 5 seconds
filterwarnings =
    ignore::DeprecationWarning
