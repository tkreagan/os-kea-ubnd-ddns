# Copy to tests/.env and fill in values before running integration tests.
# .env is gitignored; .env.example is committed.
OPNSENSE_HOST=orbison.plhm.rgn.cm
OPNSENSE_API_KEY=<your-key-id>
OPNSENSE_API_SECRET=<your-key-secret>
OPNSENSE_SSH_USER=tkr
